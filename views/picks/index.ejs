<%- include('../partials/header') %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
</head>
<table id="list">
  <thead>
    <tr>
      <th>Picker</th>
      <th>Team</th>
      <th>Spread</th>
      <th>Detials</th>
      <th>Delete Pick</th>
    </tr>
  </thead>
  <tbody>
    <% picks.forEach(function(p) { %>
      <tr>
        <td><%= p.picker %></td>
        <td><%= p.title %></td>
        <td><%= p.releaseYear %></td>
        <td><a href="/picks/<%= p._id %>">PICK DETAILS</a></td>
        <td><form class="delete-form" action="/picks/<%= p._id %>?_method=DELETE" method="POST">
          <button type="submit">X</button>
      </form></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>
